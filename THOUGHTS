#DATA INPUT: a preformated excel file, with the top being the gene of interest, and each cell being the amplification data, the left hand side is the sample ID.
#STAT: ANOVA and TukeyHSD.
#Language: Python, use VScode jupyter notebook

#pipeline:
    #data import: function to load and preprocess qPCR data
        #some kind of normalization method to adjest difference
        #handling missing value and outliers?
    #VISUALIZATION
        #bar plots
        #PCAs
    #export havent decieded

#user interface? Probaby Tkinter, but this will be decided after the code is done
#deplot it using pip for sure and host on github for collaboration